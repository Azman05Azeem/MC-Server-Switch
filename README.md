# MC-Server-Switch
On a self-hosted Minecraft server, this app will conserve power by timing-out the server. If a players want to join the server while it's off, they can go to their SMP Discord server and use a single command to run the server, assuming that the host runs the script.
